-- Template Script Kaitun Grow a Garden 2
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

print("Script Grow a Garden 2 berhasil dimuat!")

-- Masukkan logika auto-farm atau otomatisasi kamu di sini
task.spawn(function()
    while task.wait(1) do
        -- Contoh: kode otomatisasi berjalan di background tanpa UI
    end
end)
